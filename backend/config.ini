[SNOWFLAKE]

# The Snowflake account name (redacted example)
account = REDACTED

# The Snowflake user email/service account (redacted example)
user = REDACTED

; Path to the Snowflake private key.
; Can be absolute, or relative to the backend/ directory.
private_key_file = private_key_snowflake.pem

# Private key passphrase (redacted example)
private_key_file_pwd = REDACTED

warehouse = REDACTED
database = REDACTED
schema = REDACTED
role = REDACTED
table_name = REDACTED

[SETTINGS]
; Seconds between cache refresh cycles (background thread).
cache_refresh_seconds = 10000

; Path to the Network-Rail slide-event CSV produced by pipeline_165004.
; Relative paths are resolved against backend/.
slide_csv_path = data/all_trains_summary.csv
