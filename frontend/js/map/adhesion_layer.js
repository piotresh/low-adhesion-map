/**
 * Placeholder for the "Adhesion" map overlay.
 *
 * Kept as no-ops so switching the "Map Display" radio to `adhesion` doesn't
 * throw. Real rendering is a future feature; the original code already had
 * these stubs and we preserve them here.
 */

function showAdhesionMapLayer() {
  console.log("ℹ️ Adhesion layer function placeholder");
}

function hideAdhesionMapLayer() {
  console.log("ℹ️ Adhesion layer hide placeholder");
}
